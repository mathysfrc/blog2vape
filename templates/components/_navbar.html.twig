<nav class="navbar navbar-expand-lg">
	<div class="container-fluid">
		<button class="navbar-toggler bg-white mx-5" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<a class="navbar-brand mx-3" href="{{ path('home_index')}}">
			<img class="logo-nav img-fluid" src=" {{ asset('build/images/logo.jpeg') }} " alt="Logo Blog2Vape">
        </a>
		</div>
		<div class="collapse navbar-collapse justify-content-end mx-5" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link mx-5 fs-4 fw-bold" href="{{ path('app_presentation') }}">Présentation</a>
				</li>
				<li class="nav-item">
					<a class="nav-link mx-5 fs-4 fw-bold" href="{{ path('app_vape') }}">Vapes</a>
				</li>
				{% if app.user %}
				<li class="nav-item">
					<a class="nav-link mx-5 fs-4 fw-bold" href="{{ path('app_vape_admin_index')}}">Admin</a>
				</li>

				<li class="nav-item">
					<a class="nav-link mx-5 fs-4 fw-bold" href="{{ path('app_logout')}}">Déconnexion</a>
				</li>

				{% else %}
					<li class="nav-item">
					<a class="nav-link mx-5 fs-4 fw-bold" href=" {{ path('app_login') }}">Connexion</a>
				</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
