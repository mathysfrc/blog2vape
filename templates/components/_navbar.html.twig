<nav class="navbar navbar-expand-lg">
	<div class="container-fluid">
		<button class="navbar-toggler bg-white mx-3" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<a class="navbar-brand mx-3" href="{{ path('home_index')}}">
			<img class="logo-nav img-fluid" src=" {{ asset('build/images/logo.jpeg') }} " alt="Logo Blog2Vape">
		</a>
		<div class="btn-group dropstart order-md-2">
			<button type="button" class="btn" data-bs-toggle="dropdown" aria-expanded="false">
				<a class="mx-3">
					<img class="profil-nav" src=" {{ asset('build/images/profil.png') }} " alt="Section Profil">
				</a>
			</button>
			<ul class="dropdown-menu">

				{% if (is_granted('ROLE_ADMIN')) %}
					<li class="nav-item">
						<a class="nav-link mx-5 my-3 fs-4 fw-bold text-black" href="{{ path('app_vape_admin_index')}}">Admin</a>
					</li>
				{% endif %}
				{% if app.user and not (is_granted('ROLE_ADMIN')) %}
					<li class="nav-item">
						<a class="nav-link mx-5 my-3 fs-4 fw-bold text-black" href=" {{ path('app_favourite') }}">Favoris</a>
					</li>
				{% endif %}
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link mx-5 my-3 fs-4 fw-bold text-black" href="{{ path('app_logout')}}">Déconnexion</a>
					</li>
				{% endif %}
				{% if not app.user %}

					<li class="nav-item">
						<a class="nav-link mx-5 my-3 fs-4 fw-bold text-black" href="{{ path('app_register') }}">Inscription</a>
					</li>

					<li class="nav-item">
						<a class="nav-link mx-5 my-3 fs-4 fw-bold text-black" href=" {{ path('app_login') }}">Connexion</a>
					</li>
				{% endif %}
			</ul>
		</div>
		<div class="navbarCol collapse navbar-collapse justify-content-end" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link mx-5 fs-4 fw-bold" href="{{ path('app_presentation') }}">Présentation</a>
				</li>
				<li class="nav-item">
					<a class="nav-link mx-5 fs-4 fw-bold" href="{{ path('app_vape') }}">Vapes</a>
				</li>
				<li class="nav-item">
					<a class="nav-link mx-5 fs-4 fw-bold" href="{{ path('app_quizz') }}">Quizz</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
